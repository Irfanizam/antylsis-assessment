{
  "name": "loyalty-program",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack loyalty application — receipts in, vouchers out.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:migrate": "npm --prefix server run prisma:migrate",
    "db:seed": "npm --prefix server run seed",
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
